{
  "name": "wind-im",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compose": "docker compose up --build",
    "dev": "npm run dev"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}